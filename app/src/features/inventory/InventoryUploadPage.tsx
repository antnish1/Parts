import { PageCard } from '../../components/ui/PageCard';

export function InventoryUploadPage() {
  return <PageCard eyebrow="Inventory" title="Inventory Upload" description="Inventory upload module placeholder for Excel import, validation, staging, and stock lookup." />;
}
