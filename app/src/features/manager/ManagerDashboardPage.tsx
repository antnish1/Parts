import { PageCard } from '../../components/ui/PageCard';

export function ManagerDashboardPage() {
  return <PageCard eyebrow="Manager" title="Manager Dashboard" description="Dashboard module placeholder for KPI cards, branch summaries, and status metrics." />;
}
