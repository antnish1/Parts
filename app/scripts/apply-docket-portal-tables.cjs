require('./apply-portal-cutover-remaining.cjs');

// Docket service portal table replacements are now committed directly in source.
// Kept as a no-op so existing predev/prebuild chains remain stable.
